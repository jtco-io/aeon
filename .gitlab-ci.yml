variables:
  DOCKER_DRIVER: overlay2

stages:
#  - fasttest
  - build_test_deploy
#
#fasttest:
#  stage: fasttest
#  image: node:latest
#  # This folder is cached between builds
#  # http://docs.gitlab.com/ce/ci/yaml/README.html#cache
#  cache:
#    paths:
#    - node_modules/
#  script:
#   - yarn install
#   - yarn run lint
#   - yarn run flow check

build_test_deploy:
  stage: build_test_deploy
  image: docker:latest
  services:
    - docker:dind
  variables:
    IMAGE_NAME: $CI_REGISTRY_IMAGE:$CI_COMMIT_REF_NAME
  cache:
    paths:
    - cache/apk
    - cache/pip
  before_script:
    - mkdir cache/apk cache/pip
    - ln -s /var/cache/apk cache/apk/
    - apk update
    - apk add py-pip yarn
    - pip install --cache-dir cache/pip docker-compose
#    - pip install docker-compose
#    - docker login -u gitlab-ci-token -p $CI_BUILD_TOKEN $CI_REGISTRY
#    - docker build --build-arg BASE_IMAGE=$SERVER_IMAGE --target build -t $IMAGE_NAME .
  script:
     - ls  /var/cache/pip
#    - yarn run d:test
#  after_script:
#    - docker push $IMAGE_NAME
